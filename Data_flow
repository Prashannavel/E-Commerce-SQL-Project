# Data Flow Architecture

Raw CSV data imported into ecomm table.

Data cleaning performed (null handling, datatype correction).

Primary key and constraints applied.

Revenue column calculated.

KPIs generated using aggregate queries.

Stored procedures handle data insertion with validation.

Triggers enforce business rules.

Transactions ensure data integrity.
